console.log("**** 漢数字変換(1桁のみ) ****");

const readline = require('readline'); 

const rl = readline.createInterface({ 

  input: process.stdin, 

  output: process.stdout 

}); 

rl.question('数: ', (answer) => { 

  const CnineseNumeral = unknown
  if(answer = 0) {
    unknown = "零"
  }
  else if(answer = 1) {
    unknown = "一"
  }
  else if(answer = 2) {
    unknown = "二"
  }
    console.log(CnineseNumeral); 

  rl.close(); 

}); 